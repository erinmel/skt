<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:skt.IDE.ViewModels"
             x:Class="skt.IDE.Views.ToolWindows.Toolbar"
             x:DataType="vm:MainWindowViewModel">

    <Border Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
            x:Name="TitleBar">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo and File Operations -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center" Name="LogoPanel">
                <Image Source="{DynamicResource Icon.SktFile}" Width="30" Height="30" VerticalAlignment="Center"/>

                <!-- File Menu Dropdown -->
                <DropDownButton Name="FileMenuDropDown"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="10,5">
                    <DropDownButton.Content>
                        <Image Source="{DynamicResource Icon.ToolbarMenu}" Width="20" Height="20"/>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <MenuFlyout>
                            <MenuItem Header="New Project" Click="NewProjectButton_Click">
                                <MenuItem.Icon>
                                    <Image Source="{DynamicResource Icon.ToolbarProjectNew}" Width="16" Height="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Open Project" Click="OpenProjectButton_Click">
                                <MenuItem.Icon>
                                    <Image Source="{DynamicResource Icon.ToolbarProjectOpen}" Width="16" Height="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="New File" Click="NewFileButton_Click">
                                <MenuItem.Icon>
                                    <Image Source="{DynamicResource Icon.ToolbarFileNew}" Width="16" Height="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="Settings" Click="SettingsButton_Click">
                                <MenuItem.Icon>
                                    <Image Source="{DynamicResource Icon.ToolbarSettings}" Width="16" Height="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>

                <!-- Compiler Menu Dropdown -->
                <DropDownButton Name="CompilerMenuDropDown"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="10,5">
                    <DropDownButton.Content>
                        <TextBlock Text="Compiler"/>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <MenuFlyout>
                            <MenuItem Header="Lexical" Name="LexicalAnalysisMenuItem" Click="LexicalAnalysisMenuItem_Click"/>
                            <MenuItem Header="Syntactic" Name="SyntacticAnalysisMenuItem" Click="SyntacticAnalysisMenuItem_Click" IsEnabled="False"/>
                            <MenuItem Header="Semantic" Name="SemanticAnalysisMenuItem" Click="SemanticAnalysisMenuItem_Click" IsEnabled="False"/>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>

                <!-- Open Project Button -->
                <Button Name="OpenProjectButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="10,5"
                        ToolTip.Tip="Open Project"
                        Click="OpenProjectButton_Click">
                    <Image Source="{DynamicResource Icon.ToolbarProjectOpen}" Width="20" Height="20"/>
                </Button>

                <!-- New Project Button -->
                <Button Name="NewProjectButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="10,5"
                        ToolTip.Tip="New Project"
                        Click="NewProjectButton_Click">
                    <Image Source="{DynamicResource Icon.ToolbarProjectNew}" Width="20" Height="20"/>
                </Button>

                <!-- New File Button -->
                <Button Name="NewFileButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="10,5"
                        ToolTip.Tip="New File"
                        Click="NewFileButton_Click">
                    <Image Source="{DynamicResource Icon.ToolbarFileNew}" Width="20" Height="20"/>
                </Button>


                <!-- Save Button -->
                <Button Name="SaveButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="10,5"
                        ToolTip.Tip="Save"
                        Click="SaveButton_Click">
                    <Image Source="{DynamicResource Icon.ToolbarSave}" Width="20" Height="20"/>
                </Button>

                <!-- Save As Button -->
                <Button Name="SaveAsButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="10,5"
                        ToolTip.Tip="Save As"
                        Click="SaveAsButton_Click">
                    <Image Source="{DynamicResource Icon.ToolbarSaveAs}" Width="20" Height="20"/>
                </Button>
            </StackPanel>

            <!-- Draggable Area -->
            <Border Grid.Column="1" Background="Transparent" x:Name="DragArea"/>

            <!-- Your Custom Window Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0">
                <Button Name="SettingsButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="10,5"
                        ToolTip.Tip="Settings"
                        Click="SettingsButton_Click">
                    <Image Source="{DynamicResource Icon.ToolbarSettings}" Width="16" Height="16"/>
                </Button>

                <!-- Transparent spacer to separate settings from window controls -->
                <Border Width="10" Background="Transparent"/>

                <Button Name="Minimize"
                        Tag="WindowControl"
                        ToolTip.Tip="Minimize"
                        Click="Minimize_Click">
                    <Image Source="{DynamicResource Icon.Minimize}" Width="16" Height="16"/>
                </Button>


                <Button Name="MaximizeRestoreButton"
                        Tag="WindowControl"
                        ToolTip.Tip="{Binding WindowStateButtonTooltip}"
                        Click="Restore_Click">
                    <Image Source="{Binding WindowStateIcon}" Width="16" Height="16"/>
                </Button>

                <Button Name="CloseButton"
                        Tag="WindowControl"
                        ToolTip.Tip="Close"
                        Click="Close_Click">
                    <Image Source="{DynamicResource Icon.Close}" Width="16" Height="16"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>

</UserControl>